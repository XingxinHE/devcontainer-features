{
    "name": "Robotpkg",
    "id": "robotpkg",
    "version": "1.0.0",
    "description": "Sets up the robotpkg repository and installs optional packages.",
    "options": {
        "packages": {
            "type": "string",
            "default": "",
            "description": "Comma-separated list of robotpkg packages to install (e.g. robotpkg-pinocchio)."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils:2"
    ]
}